#!/usr/bin/env bash

PROJECT_DIR="$(cd -- "$(dirname "${BASH_SOURCE}")" ; cd .. > /dev/null 2>&1 && pwd -P)"

./bin/ansible-playbook \
  -i ../madek/deploy/inventories/zhdk/hosts.yml \
  -l test \
  deploy/deploy_play.yml \
  -e 'run_service_on_deploy=true' \
  "$@"
